/** Google's OAuth branding review requires the name on the consent screen to
   match the name on the homepage, so both read from here. Keep it in step
   with the App name field in the Google Auth Platform console, the <title>
   in index.html, and public/manifest.webmanifest. */
export const APP_NAME = 'Gym Performance Manager';
